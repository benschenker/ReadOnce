extends layout

block content
  p.lead Secret text here (savor it as it will not last!)
  if error
    p#msg(style = 'color:red')=error
  else
    code#msg= secret
  br
  p.lead Paste text below and generate a link that will self destruct after it is accessed once.
    div#inputarea
      code(class='secretlink', style='display:none; width:500px')
        a#secreta
      textarea#input
  br
  button#Generate Generate Link
